Should I remove the <bool> type from the variant to avoid confusion?
type would be only NOMAD::Double or std::string.

When reading file, convert to NOMAD::Double or other, when relevant. Right now all are strings.

Output to file.

Connect this with Runner.
